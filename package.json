{
  "name": "nutt-festas-website",
  "version": "1.0.0",
  "description": "Website da Nutt Festas com painel administrativo",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "prod": "cd backend && npm install && npm run prod",
    "build": "cd backend && npm install",
    "vercel-build": "cd backend && npm install",
    "dev": "cd backend && npm install && npm run dev",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": "20.x",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "website",
    "festas",
    "admin",
    "gallery"
  ],
  "author": "Gabriel",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.12.2",
    "mongoose": "^8.0.3",
    "vercel": "^48.1.6"
  }
}
